name: SonarQube Analysis

on:
  push:
    branches:
      - main
      - master
  pull_request:
    branches:
      - main
      - master
  workflow_dispatch:

jobs:
  sonarqube:
    name: Analyse SonarQube
    runs-on: self-hosted
    
    steps:
      - name: 📥 Checkout du code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
      
      - name: 🔍 Informations
        run: |
          echo "=========================================="
          echo "🚀 Analyse SonarQube"
          echo "=========================================="
          echo "📁 Répertoire: $(pwd)"
          echo "🌿 Branche: ${{ github.ref_name }}"
          echo "👤 Auteur: ${{ github.actor }}"
          ls -la
      
      - name: 🔌 Test SonarQube
        run: |
          curl -s ${{ secrets.SONAR_HOST_URL }}/api/system/status || echo "SonarQube non accessible"
      
      - name: 📊 Analyse SonarQube
        run: |
          export PATH=$PATH:/opt/sonar-scanner/bin
          sonar-scanner \
            -Dsonar.projectKey=projet_python \
            -Dsonar.projectName="Projet Python" \
            -Dsonar.sources=. \
            -Dsonar.host.url=${{ secrets.SONAR_HOST_URL }} \
            -Dsonar.token=${{ secrets.SONAR_TOKEN }} \
            -Dsonar.exclusions=venv/**,__pycache__/**,*.pyc,instance/**,dump.sql,.github/**,.scannerwork/** \
            -Dsonar.python.version=3.8
      
      - name: ✅ Terminé
        if: always()
        run: |
          echo "✅ Analyse terminée !"
          echo "📊 Résultats: ${{ secrets.SONAR_HOST_URL }}/dashboard?id=projet_python"
