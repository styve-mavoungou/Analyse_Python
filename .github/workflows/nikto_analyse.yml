name: Analyse de sécurité Nikto

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  nikto-scan:
    runs-on: ubuntu-latest
    steps:
      - name: 🔹 Vérification du code
        uses: actions/checkout@v3

      - name: 🔹 Installation de Python et dépendances
        run: |
          python3 -m venv venv
          source venv/bin/activate
          pip install -r requirements.txt

      - name: 🔹 Lancement du serveur Flask
        run: |
          nohup python3 app.py &
          sleep 10  # Attend que le serveur démarre

      - name: 🔹 Installation de Nikto
        run: |
          sudo apt update
          sudo apt install -y nikto

      - name: 🔹 Analyse de l’application avec Nikto
        run: |
          nikto -h http://127.0.0.1:5000 -output nikto_report.txt

      - name: 📦 Sauvegarde du rapport
        uses: actions/upload-artifact@v3
        with:
          name: rapport-nikto
          path: nikto_report.txt
